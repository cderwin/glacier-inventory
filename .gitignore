node_modules/
public/
npm-debug.log*
.DS_Store

# Local settings such as MAPBOX_ACCESS_TOKEN; see .env.example.
.env

# Large source dataset. `npm run prepare-data` turns it into the smaller
# app/assets/data/glaciers.geojson, which is committed.
data/*.geojson
